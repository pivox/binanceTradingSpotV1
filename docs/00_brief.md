Mise a jour des tickets:
- TL-08: ajout contrainte "attentions Temporal" conteneurisees, et obligation de configuration dans `docker-compose.yml`.
- Tache + critere d'acceptation relies a la declaration de Temporal dans `docker-compose.yml`.
- Question ouverte maintenue sur le perimetre exact des "attentions Temporal".
