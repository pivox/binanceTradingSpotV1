# AGENTS.md

## Working agreements
- All deliverables are written under /docs (Markdown).
- Never change product scope: if info is missing, list open questions + assumptions.
- For code changes: run tests and include commands + outputs in the dev log.
- For QA: every bug must include repro steps, expected vs actual, and severity.